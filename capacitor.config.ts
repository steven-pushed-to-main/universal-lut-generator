import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.universallutgenerator.app',
  appName: 'universal-lut-generator',
  webDir: 'dist'
};

export default config;
